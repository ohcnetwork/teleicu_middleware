<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../../partials/head.ejs', { title: 'Teleicu Middleware | Update Asset'})
    %>
  </head>
  <body class="max-w-screen-md mx-auto">
    <%- include("../../partials/header") %>
    <main class="py-8 w-full">
        <a
        href="/assets"
        class="btn transition bg-white text-primary-500 flex items-center text-lg"
      >
        <i data-feather="chevron-left"></i>
        Back To Assets
      </a>
      <h1 class="my-3 py-3 text-4xl border-b">Delete Asset</h1>

      <script>
        feather.replace();
      </script>

      <form action="/assets/<%- asset.externalId %>/delete" class="w-2/3 mx-auto" method="post">
        <input type="hidden" name="csrfToken" value="<%= csrfToken %>" />
        <div class="flex flex-col mt-2">
            <label for="externalId">External ID</label>
            <input class="px-2 py-1 border-2 border-black" type="text" placeholder="External ID" name="externalId" id="externalId" value="<%- asset.externalId %>" readonly/>
        </div>
        <div class="flex flex-col mt-2">
            <label for="name">Name</label>
            <input class="px-2 py-1 border-2 border-black" type="text" placeholder="Asset name" name="name" id="name" value="<%- asset.name %>" readonly/>
        </div>
        <div class="flex flex-col mt-2">
            <label for="description">Description</label>
            <textarea class="px-2 py-1 border-2 border-black" placeholder="Description" name="description" id="description" readonly><%- asset.description %></textarea>
        </div>
        <div class="flex flex-col mt-2">
            <label for="ipAddress">IP Address</label>
            <input class="px-2 py-1 border-2 border-black" type="text" placeholder="127.0.0.1" name="ipAddress" id="ipAddress" value="<%- asset.ipAddress %>" readonly/>
        </div>
        <div class="flex w-full mt-2 gap-x-2">
            <a class="w-full text-center px-4 py-2 hover:bg-blue-700 bg-blue-500"  href="/assets/">Cancel</a>
            <input class="w-full px-4 py-2 hover:bg-red-700 bg-red-500" type="submit" value="Delete" />
        </div>
      </form>
    </main>
    <%- include("../../partials/footer") %>
  </body>
</html>
