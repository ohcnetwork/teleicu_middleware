<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../../partials/head.ejs', { title: 'Teleicu Middleware | Delete Bed'})
    %>
  </head>
  <body class="max-w-screen-md mx-auto">
    <%- include("../../partials/header") %>
    <main class="py-8 w-full">
        <a
        href="/beds"
        class="btn transition bg-white text-primary-500 flex items-center text-lg"
      >
        <i data-feather="chevron-left"></i>
        Back To Beds
      </a>
      <h1 class="my-3 py-3 text-4xl border-b">Delete Bed</h1>

      <script>
        feather.replace();
      </script>

      <form action="/beds/<%- bed.id %>/delete" class="w-2/3 mx-auto" method="post">
        <input type="hidden" name="csrfToken" value="<%= csrfToken %>" />
        <div class="flex flex-col mt-2">
            <label for="name">Name</label>
            <input class="px-2 py-1 border-2 border-black" type="text" placeholder="Bed name" name="name" id="name" value="<%- bed.name %>" readonly/>
        </div>
        <div class="flex flex-col mt-2">
            <label for="externalId">External ID</label>
            <input class="px-2 py-1 border-2 border-black" type="text" placeholder="UUID" name="externalId" id="externalId" value="<%- bed.externalId %>" readonly/>
        </div>
        <div class="flex w-full mt-2 gap-x-2">
            <a class="w-full text-center px-4 py-2 hover:bg-blue-700 bg-blue-500"  href="/beds">Cancel</a>
            <input class="w-full px-4 py-2 hover:bg-red-700 bg-red-500" type="submit" value="Delete" />
        </div>
      </form>
    </main>
    <%- include("../../partials/footer") %>
  </body>
</html>